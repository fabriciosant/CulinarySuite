<%= form_with(model: c_tipo_pagamento) do |form| %>
  <% if c_tipo_pagamento.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(c_tipo_pagamento.errors.count, "error") %> prohibited this c_tipo_pagamento from being saved:</h2>

      <ul>
        <% c_tipo_pagamento.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :descricao, style: "display: block" %>
    <%= form.text_field :descricao %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
